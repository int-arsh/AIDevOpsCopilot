# Async web framework for REST/WebSocket APIs, request validation, and OpenAPI docs.
fastapi==0.136.1

# ASGI server to run the FastAPI app in development and production containers.
uvicorn==0.47.0

# Multi-agent orchestration: StateGraph, checkpoints, and routing for DevOps workflows.
langgraph==1.2.0

# Shared LangChain abstractions (messages, runnables) used by LangGraph nodes and tools.
langchain-core==1.4.0

# Official Groq Python SDK for calling llama-3.3-70b-versatile and other hosted models.
groq==1.2.0

# Kubernetes Python client for in-cluster or kubeconfig-based API access from tools/agents.
kubernetes==35.0.0

# Async-capable HTTP client for Prometheus queries, GitHub REST calls, and other integrations.
httpx==0.28.1

# Load secrets and configuration from a local .env file without hardcoding keys in code.
python-dotenv==1.2.2

# GitHub API wrapper for creating issues, reading repos, and automating DevOps follow-ups.
PyGithub==2.9.1

# Test runner and fixtures for unit and integration tests across the codebase.
pytest==9.0.3

# Pytest plugin enabling async tests and event-loop fixtures for FastAPI and httpx-heavy code.
pytest-asyncio==1.3.0
