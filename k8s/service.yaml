apiVersion: v1
kind: Service
metadata:
  name: devops-copilot-svc
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: devops-copilot
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP